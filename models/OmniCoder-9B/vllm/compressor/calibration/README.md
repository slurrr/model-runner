Replace this placeholder with a real local calibration dataset file or override `dataset_path` on the CLI.

Supported local inputs should be formats that Hugging Face datasets can read directly, such as JSON/JSONL, CSV, or Parquet.

