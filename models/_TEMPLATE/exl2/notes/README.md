# EXL2 Model Notes Template

Use this as a starting point for `models/<model_name>/exl2/notes/README.md`.

## Where
- Local path:
- Runner: `tui.py` (`--backend exl2`)

## What it is
- Base architecture (if known):
- Quantization / format notes:

## Templates
- Active template file:
- Notes about role markers / special tokens:
- Any history sanitation (e.g. strip pre-`</think>`):

## Recommended commands
- `tui --backend exl2 "<model_dir>" ...`

## Known issues / symptoms
- Symptom:
- Likely cause:
- Fix / workaround:

