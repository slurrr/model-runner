# Ollama Model Notes Template

Use this as a starting point for `models/<model_name>/ollama/notes/README.md`.

## Where
- Ollama model name:
- Host notes (WSL/Windows):

## Recommended commands
- `python ollama_chat.py "<model>" ...`

## Thinking / formatting
- Does it emit `<think>` blocks?
- Do you want to show/hide them by default?

## Known issues / symptoms
- Symptom:
- Likely cause:
- Fix / workaround:
