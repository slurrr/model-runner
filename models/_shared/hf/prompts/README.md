# Shared prompts (HF)

Put shared, non-secret prompt assets here (few-shots, tool schema examples, etc.).

Note: this repo is configured to ignore prompt content by default to avoid accidentally committing secrets.
Only this `README.md` is tracked under `prompts/`.
