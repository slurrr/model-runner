# ds32b-base templates (gguf)

This folder is for prompt/template overrides or experiments.

Suggested starting point:
- `ds32b_min_sanitized.jinja`: minimal role template + strips pre-`</think>` from assistant history
